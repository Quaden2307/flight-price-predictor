# May 11, 2026

Pipeline has been running smoothly for two days, about 17K rows of clean data, 0 failures across all runs. Decided to skip modeling on DB1B for now since the data shape is too different (quarterly route averages vs individual round-trip prices) and the live data has more useful columns to work with anyway — return_at, trip_duration_days, lead_time_days, transfers, etc.

Plan is to build a data pipeline in Jupyter that pulls from flights.db and generates charts. Going to spend time thinking through which charts actually matter for modeling before writing any code, since I don't want to just plot everything. Probably price vs lead_time, price by day-of-week of departure, boxplots by trip_duration bucket, and price by airline at minimum. Each chart should answer one specific question about what the eventual model needs to learn. Plan is to start with one chart, get it right, then add more.
