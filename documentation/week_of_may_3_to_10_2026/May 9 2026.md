# May 9, 2026

Built a data collector prototype. The collect.py file can now make calls to API and can receive flight data. Have the structure down, however need to add a lot more variable fields within params; for example, routes will be a massive decision within the project, as that may directly impact the model's training. Other parameters include more sophisticated pricing timelines, as well as class (economy business, etc.). 
